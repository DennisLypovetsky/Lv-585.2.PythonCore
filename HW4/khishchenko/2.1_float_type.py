# Create a list that contains elements 
# of integer type, then use the loop to 
# change the type of these elements to a floating type. 
# (Hint: use the built-in float () function). 


list_of_integer_elements = [1, 4, 8, 5, 3 , 5]

for i in list_of_integer_elements:
    print(float(i))
